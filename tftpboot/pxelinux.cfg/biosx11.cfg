DEFAULT menu.c32
MENU TITLE Flash BIOS
NOHALT 1
PROMPT 0
MENU IMMEDIATE

LABEL X11DAi-N
  MENU LABEL X11DAi-N (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DAi-N17.825
  endtext

LABEL X11DDW
  MENU LABEL X11DDW (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DDW7.C05
  endtext

LABEL X11DGQ
  MENU LABEL X11DGQ (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DGQ18.126
  endtext

LABEL X11DPFR
  MENU LABEL X11DPFR (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPFR8.224
  endtext

LABEL X11DPG
  MENU LABEL X11DPG (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPG7.622
  endtext

LABEL X11DPH
  MENU LABEL X11DPH (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPH7.622
  endtext

LABEL X11DPi-N
  MENU LABEL X11DPi-N (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPi-N17.609
  endtext

LABEL X11DPLI
  MENU LABEL X11DPLI (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPLI7.B29
  endtext

LABEL X11DPTB
  MENU LABEL X11DPTB (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPTB8.224
  endtext

LABEL X11DPTS
  MENU LABEL X11DPT (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPTS8.224
  endtext

LABEL X11DPU
  MENU LABEL X11DPU (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPU8.224
  endtext

LABEL X11DPUL
  MENU LABEL X11DPUL (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPUL7.B29
  endtext

LABEL X11DPUZ
  MENU LABEL X11DPUZ (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPULZ.B30
  endtext

LABEL X11DPX
  MENU LABEL X11DPX (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11DPX8.131
  endtext

LABEL X11OPi
  MENU LABEL X11OPi (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11OPi17.C05
  endtext

LABEL X11QPH
  MENU LABEL X11QPH (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11QPH18.201
  endtext

LABEL X11SAE7-330
  MENU LABEL X11SAE7-330 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SAE7-330.img

LABEL X11SAE7-518
  MENU LABEL X11SAE7-518 (SEE NOTE)
  KERNEL memdisk
  APPEND initrd=bios/X11SAE7-518.img

  text help
                                        This bios update has a
                                        red light.  To fix, use
                                        X11SAE7-330 or newer.
  endtext

LABEL X11SPL
  MENU LABEL X11SPL (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11SPL7.C05
  endtext

LABEL X11SPM
  MENU LABEL X11SPM (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11SPM7.B29
  endtext

LABEL X11SPW
  MENU LABEL X11SPW (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11SPW7.606
  endtext

LABEL X11SRA
  MENU LABEL X11SRA (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11SRA7.A11
  endtext

LABEL X11SRM
  MENU LABEL X11SRM (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11SRM7.B07
  endtext

LABEL X11SSD7-831
  MENU LABEL X11SSD7-831 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSD7-831.img

LABEL X11SSH7-A06
  MENU LABEL X11SSH7-A06 2.0c (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSH7-A06.img

LABEL X11SSH7-309
  MENU LABEL X11SSH7-309 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSH7-309.img

LABEL X11SSH7-727
  MENU LABEL X11SSH7-727 2.0b (OLD)
  KERNEL memdisk
  APPEND initrd=bios/X11SSH7-727.img

LABEL X11SSH7-A06
  MENU LABEL X11SSH7-A06 2.0c
  KERNEL memdisk
  APPEND initrd=bios/X11SSH7-A06.img

LABEL X11SSL7-315
  MENU LABEL X11SSL7-315 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSL7-315.img

LABEL X11SSLF7-A06
  MENU LABEL X11SSLF7-A06 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSLF7-A06.img

LABEL X11SSLF7-106
  MENU LABEL X11SSLF7-106 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSLF7-106.img

LABEL X11SSN
  MENU LABEL X11SSN (UEFI SHELL)

  text help
                                        This bios requires using
                                        the UEFI shell. Example:
                                          flash.nsh X11SSN7.B22
  endtext

LABEL X11SSV6-C23
  MENU LABEL X11SSV6-C23 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSV6-C23.img

LABEL X11SSVQ7-B06
  MENU LABEL X11SSVQ-B06 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSVQ-B06.img

LABEL X11SSW7-310
  MENU LABEL X11SSW7-310 (UNTESTED)
  KERNEL memdisk
  APPEND initrd=bios/X11SSW7-310.img

LABEL back
  MENU LABEL Go back to ^previous menu
  KERNEL menu.c32
  APPEND pxelinux.cfg/mbbios.cfg
